# Sample memory

This is an example memory file. Point **Settings → Memory folder** at this directory
(`examples/memory/`) to see how Janus loads context.

- The user prefers concise, direct answers.
- Project "Janus" is a Tauri desktop assistant with a local↔Claude router.
- When escalating to Claude, the local model compresses this memory first to save tokens.
